@model PhotoContest.Models.Models.Contest
@{
    ViewBag.Title = "Delete";
}

<h2 class="text-right">Delete</h2>
<hr />
<div class="col-md-8 col-md-offset-8">
    @using (Html.BeginForm("DeleteContest", "Contests", FormMethod.Post))
    {
        <div class="row">
            <div class="panel panel-primary">
                <div class="panel-heading"><h3>Information</h3></div>
                <div class="panel-body">
                    <div class="container-fluid">

                        <div class="list-group">
                            <div class="list-group-item">
                                @Html.Label("title", "Title: ")
                                @Html.TextBox("title", null, new { @class = "form-control", disabled = "disabled" })
                            </div>
                            <div class="list-group-item">
                                @Html.Label("description", "Description: ")
                                @Html.TextBox("description", null, new { @class = "form-control", disabled = "disabled" })
                            </div>
                            <div class="list-group-item">
                                <b>Contest Status: </b>
                                @Html.TextBox("status", null, new { @class = "form-control", disabled = "disabled" })
                            </div>
                            <div class="list-group-item">
                                <b>Reward Strategy: </b>
                                @Html.TextBox("rewardstrategy", null, new { @class = "form-control", disabled = "disabled" })
                            </div>
                            <div class="list-group-item">
                                @Html.Label("deadline", "Deadline: ")
                                @Html.TextBox("deadline", null, new { @class = "form-control", disabled = "disabled" })
                            </div>
                        </div>
                        @Html.Hidden("Id", @Model.Id)
                    </div>
                </div>
            </div>
            <input type="submit" value="Delete Contest" class="btn btn-success pull-left" />
            @Html.ActionLink("Cansel", "View", "Contests", new { id = @Model.Id }, new { @class = "btn btn-danger pull-right" })
        </div>
    }
</div>

